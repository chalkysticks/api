{
	"devDependencies": {
		"axios": "^1.6.4",
		"laravel-vite-plugin": "^1.0",
		"vite": "^5.0"
	},
	"private": true,
	"scripts": {
		"build": "vite build",
		"db:seed": "php artisan db:seed",
		"db:wipe": "yarn migrate:fresh && yarn db:seed",
		"dev": "vite",
		"migrate": "php artisan migrate",
		"migrate:fresh": "php artisan migrate:fresh",
		"serve": "php artisan serve"
	},
	"type": "module"
}
